# Phase 5 — Execution engine (IB Gateway, database migrations, Docker orchestration)
# Install on top of requirements.txt + requirements-phase4.txt when building execution layer
alembic==1.18.4
Mako==1.3.12
SQLAlchemy==2.0.51
sqlparse==0.5.5
docker==7.1.0
